
%%% WARNING %%%
%% Dont mess with this stuff unless you know what you are doing
%%%%%%%%%%%%%%%

\usepackage{graphicx}  %for images and plots
\usepackage[letterpaper, left=1.25in, right=1.25in, top=1.25in, bottom=1.25in]{geometry}
\usepackage{setspace}  %use this package to set linespacing as desired
\usepackage{times}  %set Times New Roman as the font
\usepackage[explicit]{titlesec}  %title control and formatting
\usepackage[titles]{tocloft}  %table of contents control and formatting
\usepackage[bookmarks=true, hidelinks]{hyperref}
\usepackage[page]{appendix}  %for appendices
\usepackage{rotating}  %for rotated, landscape images
\usepackage[normalem]{ulem}  %for underlined section titles
\usepackage{dcolumn}% Align table columns on decimal point
\usepackage{bm}
\usepackage{graphicx}
\usepackage{braket}
\usepackage{amsmath}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{dcolumn}% Align table columns on decimal point
\usepackage{tabularx}
\usepackage{fancyhdr}
\usepackage{placeins}
\usepackage{booktabs}
\usepackage{lipsum}

%\setmonofont{Latin Modern Mono}

\usepackage[autostyle=true]{csquotes}

\usepackage{fontspec}
\setmainfont{DejaVu Serif}
% \usepackage{ragged2e}
% \usepackage{unicode-math}
% \usepackage{noto}
% %\setmathfont[Scale=MatchUppercase]{libertinusmath-regular.otf}
% \setmainfont[BoldFont={Noto Sans Mono Bold}]{Noto Sans Mono}

\usepackage[backend=biber, sorting=none, bibstyle=ieee,sortcites=True]{biblatex}  %reference manager

%\usepackage{libertine}
% \DeclareMathAlphabet{\mathcal}{OMS}{cmsy}{m}{n}
% \let\mathbb\relax % remove the definition by unicode-math
% \DeclareMathAlphabet{\mathbb}{U}{msb}{m}{n}
% \let\mathit\relax % remove the definition by unicode-math
% \DeclareMathAlphabet{\mathit}{U}{msb}{m}{n}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[R]{\thepage}
\setlength{\headheight}{15pt}
% my troubleshoot
\makeatletter
\let\ps@plain\ps@fancy % plain style = fancy style
\makeatother

%Add your bibliography file here
\bibliography{references}

% prevent certain fields in references from printing in bibliography
\AtEveryBibitem{\clearfield{issn}}
\AtEveryBibitem{\clearlist{issn}}

\AtEveryBibitem{\clearfield{language}}
\AtEveryBibitem{\clearlist{language}}

\AtEveryBibitem{\clearfield{doi}}
\AtEveryBibitem{\clearlist{doi}}

\AtEveryBibitem{\clearfield{url}}
\AtEveryBibitem{\clearlist{url}}

\AtEveryBibitem{%
  \ifentrytype{online}
    {}
    {\clearfield{urlyear}\clearfield{urlmonth}\clearfield{urlday}}}


\newcommand{\myEpigraph}[2]{\begin{center} #1\\ \textit{#2} \end{center} }
\newcommand{\myvar}[3]{\mathbf{#1}_{\text{#2}}^{\text{#3}}}
\newcommand{\myscalar}[3]{#1_{\text{#2}}^{\text{#3}}}
\renewcommand{\eqref}[1]{\mbox{Eq. (\ref{#1})}}
\newcommand{\figref}[1]{\mbox{Figure \ref{#1}}}
\newcommand{\tabref}[1]{\mbox{Table \ref{#1}}}
\newcommand{\secref}[1]{\mbox{Section \ref{#1}}}
\newcommand{\chapref}[1]{\mbox{Chapter \ref{#1}}}
\interfootnotelinepenalty=10000
%%%%%%%%%%%%%%%%%%%%%%
% Start of Document
%%%%%%%%%%%%%%%%%%%%%%
\newcommand{\onlinecite}[1]{\hspace{-1 ex} \nocite{#1}\citenum{#1}}

\usepackage{nomencl}
\makenomenclature

\usepackage{etoolbox}
\renewcommand{\nomname}{List of Symbols}
\newcommand\Nomenclature[2]{\nomenclature[#2]{#1}{#2}}

\newcommand{\sectionbreak}{\FloatBarrier}
\newcommand{\subsectionbreak}{\FloatBarrier}

% \renewcommand\nomgroup[1]{%
%   \item[\bfseries
%   \ifstrequal{#1}{A}{Target Specifications}{%
%   \ifstrequal{#1}{B}{Laser Specifications}{%
%   \ifstrequal{#1}{C}{LILAC Observables}{}{%
%   \ifstrequal{#1}{D}{Experimental Observables}{}}}}%
% ]}


\usepackage{hyperref}
